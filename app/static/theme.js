/* Theme toggle lives inline in base.html (window.__hangoutTheme) so it cannot
   fail to bind if this file is cached or blocked. Kept as a no-op stub for any
   stale HTML that still references /static/theme.js. */
